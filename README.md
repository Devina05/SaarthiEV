Try AI directly in your favourite apps … Use Gemini to generate drafts and refine content, plus get Gemini Pro with access to Google's next-gen AI
1
100%
# SaarthiEv — Tech Intern Assignment

- **[part-a/](part-a/)** — Fare Estimator API (Fastify + TypeScript): `POST /estimate`, `GET /estimate/history`, tests, README.
- **[part-b/](part-b/)** — Debugging: fixed `ride-service.ts` + [bug notes](part-b/BUGS.md) (4 bugs: haversine radians, driver-assignment race, OTP ms/seconds mixup, fare-split truncation).
- **[part-c/](part-c/)** — Bonus: Socket.io live driver location + driver/rider simulation script.

Each part's README has its run instructions. Node 20+ required.